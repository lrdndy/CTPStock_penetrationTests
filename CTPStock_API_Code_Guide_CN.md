# 连接与基础功能测试代码讲解

这份工程首先验证本机能否通过所附 CTP 个股期权 SDK 连接指定测试环境，完成客户端认证、交易账户登录及只读查询，并单独验证行情端登录。当前源码版本为 `0.2.2`，使用个股期权 SDK `v3.7.5_CP_20251125`，还包含受显式开关保护的单次限价报单/撤单基础功能测试。

默认 `connectivity` 模式仍不报单。`basic` 模式实现报单数量固定为 1 的单次 GFD 限价委托及进入排队状态后的自动撤单；默认只是 dry-run，必须同时提供 `--send-order --confirm SEND_ONE_ORDER` 才会实发。程序没有行情选价、追价、自动重试、成交后自动平仓、密码修改、结算确认或正式风控功能。

## 1. 先认识 API 和 SPI

CTP 接入由两个方向组成：

| 方向 | 代码角色 | 举例 |
| --- | --- | --- |
| 程序向 SDK 发请求 | API：SDK 已实现的接口对象 | `ReqAuthenticate`、`ReqUserLogin` |
| SDK 把结果交给程序 | SPI：程序继承并实现的回调对象 | `OnRspAuthenticate`、`OnRspUserLogin` |

可以把 API 理解成提交请求的窗口，把 SPI 理解成登记好的结果接收人。`RegisterSpi` 告诉 SDK 以后向哪个对象通知连接、成功或失败；`Init` 才真正启动 API 的工作。

交易接口和行情接口是两个独立对象，连接各自的前置地址。交易端登录成功不代表行情端也已登录，行情端登录成功也不表示已订阅或收到行情。

## 2. 为什么一定要看回调

普通同步函数通常返回最终结果，但 CTP 的请求函数先返回“请求是否被接口接受”，业务结果随后异步到达。

| 检查内容 | 来源 | 含义 |
| --- | --- | --- |
| 请求提交返回值 | `Req...(...)` 返回的 `int` | 返回 `0` 表示请求提交被接受，仍要等待响应；非零表示此次提交没有成功 |
| 业务错误码 | 响应中的 `pRspInfo->ErrorID` | `0` 表示这条响应未报告业务错误；非零需要结合错误信息定位 |
| 响应属于哪个请求 | `nRequestID` | 与发起请求时的编号匹配，避免旧响应被认成当前结果 |
| 这次响应是否结束 | `bIsLast` | 查询可能返回多条；只有最后一条到达后，才能确认该次查询完整结束 |
| 实际响应数据 | 回调中的数据指针 | 登录成功应有登录数据；查询无记录时可能是空指针，应检查后再使用 |

因此“连接成功”“请求返回 0”“业务登录成功”是三个不同的阶段。程序应当分别记录，才能知道是网络、客户端认证、账户密码还是查询出了问题。

`OnFrontConnected` 仅说明 API 与前置建立连接，不能在这里就打印“账户登录成功”。`bIsLast` 也不是成功标志：最后一条响应仍然可能带错误码。

## 3. 个股期权 SDK 与编译边界

附件中的接口属于 `ctp_sopt` 命名空间，库文件带 `sopt` 前缀，例如 `soptthosttraderapi_se.lib`。这是本工程使用的个股期权 SDK，不能因为接口类名相似就替换为期货 CTP 的 6.x SDK。

`ThostFtdcTraderApi.h` 和 `ThostFtdcMdApi.h` 定义接口；`ThostFtdcUserApiStruct.h` 定义报文结构；`ThostFtdcUserApiDataType.h` 定义字段类型、长度及枚举常量。

ABI 是“编译出的程序怎样与 DLL 对接”的约定。字段长度、顺序、对齐、命名空间、函数签名以及 32/64 位都参与这个约定。因此必须使用与 DLL 配套的头文件和 LIB，不能复制别的版本结构体来凑字段，也不能擅自增加结构体成员。

Windows 下 `.lib` 在编译链接时告诉程序如何引用 SDK，`.dll` 在运行时提供实际实现。两只 3.7.5 DLL 不存放在公开 Git 仓库，用户需先从原厂 x64 SDK 包复制到本地 `sdk/win64`。`build_windows.bat` 使用 MSVC x64 编译器，随后把两份 DLL 复制到生成的 EXE 目录；`CMakeLists.txt` 提供同一目标的 CMake 构建方式。

工程的 `sdk/win64` 保存原始 SDK 的头文件、LIB、错误码文件以及用户在本机补入的两只 DLL；`sdk/include` 提供 UTF-8 头文件副本，方便中文注释显示及统一编译编码。转码不应改变任何声明、字段或接口。

当前 3.7.5 包必须作为整体使用。其 Trader 虚函数表和部分共享结构相较 3.7.0 已有变化，因此头文件、LIB、DLL 及 `error.xml` 不能跨版本混用；`run_windows.bat --version` 输出的运行时版本是最终核对依据。

本阶段仅针对 Windows x64；不会因为原始上传压缩包还有 Linux 子包，就声称本项目已经验证 Linux 交易及行情。

## 4. `src/main.cpp` 的结构

初读代码时，建议先看 `main`，再看 `testTrader` / `testMd`，最后看 `runStage` 和 `State`。这样先理解“做什么”，再理解“怎样等待异步结果”。

| 代码对象 | 负责什么 | 为什么单独写出来 |
| --- | --- | --- |
| `Config` | 保存经纪公司、账号、投资者编号、前置地址和 AppID | 接入参数与测试逻辑分开；凭据由独立的 `Secrets` 保存 |
| `Options` | 保存运行模式、基础测试参数、配置文件路径、超时和是否跳过资金查询 | 默认连接测试与显式基础交易测试共用一个入口 |
| `parseOptions` | 解析并校验命令行、测试模式、交易所、方向、开平、价格和实发确认令牌 | 缺少完整确认时保持 dry-run，防止误发委托 |
| `readConfig(path, Secrets&)` | 读取主 INI 和可选同名本地 INI，分别填充连接参数与凭据 | 拒绝同一文件的重复键、未知键和不属于本阶段的前置环境；投资者编号留空时沿用用户编号 |
| `field` / `textField` | 在 C++ 字符串与 SDK 固定长度字符数组之间转换 | 防止输入静默截断或读取越过字段边界 |
| `Secrets` / `getSecret` | 管理配置文件读取的凭据；缺项时读取环境变量或隐藏输入 | 每项凭据独立选择来源，已配置的值不再重复询问 |
| `Logger` / `clean` / `sdkMessage` | 写控制台与文件日志、脱敏、转换 SDK 中文错误信息 | 让同一次运行能直接定位错误并留下原始证据 |
| `Stage` / `Result` | 表示当前阶段和该阶段的结果快照 | 把连接、认证、登录和查询的结果区分开 |
| `State` | 保存当前请求及结果；协调主线程与 SDK 回调线程 | 集中处理请求匹配、超时、断线和多条查询响应 |
| `OrderLifecycle` | 关联本次 `OrderRef`，等待排队、成交、拒绝和撤单状态 | 报单回报不是普通 `bIsLast` 查询，必须使用独立状态机 |
| `TraderSpi` / `MdSpi` | 覆盖 SDK 回调，将响应转交 `State` | 回调保持短小，实际流程由主线程控制 |
| `runStage` | 为一个阶段启动等待状态、提交请求、等待并报告 | 各阶段统一处理提交失败和异步失败 |
| `testTrader` / `testMd` | 组织各自的测试顺序 | 两套 API 独立诊断；基础测试只允许 trader 模式 |
| `testBasicFunction` | 生成一次策略报单；dry-run 到此停止，实发时等待排队并撤单 | 把策略生成、真实发送和撤单证据明确区分 |
| `ApiDeleter` | 注销 SPI 并调用 API 的 `Release` | 即使提前返回或发生异常，也有确定的释放路径 |
| `main` | 组合配置、凭据、日志、测试和退出码 | 整个程序的入口及总结果汇总 |

### `Config` 与程序版本不是同一件事

`kVersion` 是当前连接工具的版本。`Config::app` 是券商分配给接入软件的 AppID，它们用途不同；不能为了让显示“整齐”就自行修改 AppID。程序还记录两套 DLL 的 `GetApiVersion()` 返回值，用于核对实际载入的 SDK。

### 固定长度字段为什么不能直接随便复制

SDK 的请求结构大量使用 `char[N]`，最后需要留一个字节放字符串结束符。`field` 通过模板取得字段容量，拒绝空值、过长内容以及换行、制表符或内嵌空字符，再清零并复制。

例如代码不是先截短密码继续登录，而是在连接前明确提示字段无效。这样可以避免“配置实际有错，却一直表现成远端认证失败”。`textField` 则在字段长度范围内查找结束符，防止输出 SDK 字段时越界读取。

## 5. 交易端实际执行流程

`testTrader` 先创建 `State`，再创建持有它的 `TraderSpi`，最后创建 API 对象。它注册回调、设置公共流与私有流模式、登记前置地址，然后按以下顺序执行：

| 顺序 | 代码发起动作 | 等待的回调 | 请求编号 | 该步失败后的行为 |
| --- | --- | --- | --- | --- |
| 1 | `api->Init()` | `OnFrontConnected` | `0`，仅为程序内连接阶段编号 | 结束交易端测试 |
| 2 | `api->ReqAuthenticate(&auth, 1)` | `OnRspAuthenticate` | `1` | 结束交易端测试，不继续账户登录 |
| 3 | `api->ReqUserLogin(&login, 2)` | `OnRspUserLogin` | `2` | 结束交易端测试，不继续查询 |
| 4 | `api->ReqQryTradingAccount(&query, 3)` | `OnRspQryTradingAccount` | `3` | 记录查询失败，交易端整体失败 |
| 5（仅 basic 实发） | `api->ReqOrderInsert(&order, 4)` | `OnRtnOrder` / 报单错误回调 | `4` | 不重试；记录失败或成交事实 |
| 6（委托可撤时） | `api->ReqOrderAction(&action, 5)` | `OnRtnOrder` / 撤单错误回调 | `5` | 不重试；超时要求立即人工核对 |

认证请求包含 `BrokerID`、`UserID`、`AppID`、`AuthCode`。这是接入软件的客户端认证，之后账户登录仍需要交易密码。

`loginRequest` 把登录结构零初始化后填写经纪公司、用户和密码。它不伪造终端 IP、MAC 或硬件采集信息，也不调用中继上报接口；真实终端信息是否完整采集，仍需要在指定实体机运行并由券商核验。

资金查询只读取账户信息。程序输出查询行数、账号、币种、余额和可用资金；这些是实际返回数据，不能据此认定账户具备报单权限或已经完成交易。若接口正常结束但没有记录，日志会明确说明“空结果”，不会凭空生成账户。

`--skip-query` 会在交易登录成功后跳过资金查询。这时交易端成功的范围只到登录，不能把查询写为已通过。

代码中的 `SubscribePrivateTopic(THOST_TERT_QUICK)` 和 `SubscribePublicTopic(THOST_TERT_QUICK)` 设置交易 API 的信息流接收方式，本例不重放历史流。它们不是合约行情订阅；只有显式选择 `--test basic` 时，本程序才为本次单一 `OrderRef` 处理委托和成交回报。

### 5.1 基础功能可选流程

`--test basic` 只能与 `--mode trader` 一起使用，并要求用户明确提供合约、交易所、买卖方向、开平和限价。`basicOrderRequest` 将这些参数转成一个 `CThostFtdcInputOrderField`：限价、GFD、任意数量成交、投机标志，数量在源码中固定为 1。`OrderRef` 取交易登录响应中的 `MaxOrderRef + 1`；若该字段不是数字或没有安全空间，程序在发单前停止。

默认没有 `--send-order` 时，程序完成认证、登录、资金查询和报单结构校验，只记录：

```text
BASIC RESULT status=PASS strategy=PASS order_fields=PASS transmission=NOT_REQUESTED cancel=NOT_RUN
```

这不是柜台报单通过。只有再提供准确的 `--confirm SEND_ONE_ORDER` 才调用 `ReqOrderInsert(..., 4)`。程序不把接口即时返回 `0` 当作订单成功，而是按同一 `OrderRef` 处理 `OnRspOrderInsert`、`OnErrRtnOrderInsert`、`OnRtnOrder` 和 `OnRtnTrade`。

当 `OnRtnOrder` 显示委托仍在队列中，主线程使用回报中的 `FrontID`、`SessionID`、`ExchangeID`、`OrderSysID` 和 `OrderRef` 构造撤单，调用 `ReqOrderAction(..., 5)`。只有后续同一订单状态确认为 `Canceled`，并且确实由程序发起过撤单，才记录 `cancel=PASS`。完全成交、拒绝、非排队终态或超时都不会冒充撤单通过；若超时，日志会要求立即去柜台核对活动委托。

这个 `single-shot-limit` 是测试用的确定性单次策略，不做行情判断。实发委托仍可能在撤单到达前成交；程序会记录 `OnRtnTrade`，但不会自动发第二笔反向单。它的真实能力应按此描述，不能写成行情策略、做市、套利或完整订单管理系统。

## 6. 行情端实际执行流程

`testMd` 使用自己的 `State`、`MdSpi` 和行情 API 对象；不会共享交易端的登录状态。

| 顺序 | 发起动作 | 等待的回调 | 请求编号 |
| --- | --- | --- | --- |
| 1 | `CreateFtdcMdApi(..., false, false)` 创建 API；注册回调和前置；调用 `Init()` | `OnFrontConnected` | `0`，程序内连接阶段编号 |
| 2 | `ReqUserLogin(&login, 1)` | `OnRspUserLogin` | 请求为 `1`；当前柜台实测成功回调为 `0`，程序仅在 MD 登录兼容 |

这里两个 `false` 对应 SDK 的 UDP 与组播开关，本阶段沿用给定 TCP 前置。附件中的行情 API 没有交易端的 `ReqAuthenticate` 方法，因此 `--mode md` 只需要交易密码，不索取授权码。

本阶段没有调用 `SubscribeMarketData`，也没有处理 `OnRtnDepthMarketData`。行情登录成功只验证行情登录链路；下一阶段若要检查是否真正收到行情，还要增加合约订阅、订阅响应及行情数据验证。

`--mode all` 的意思是先测试交易端，再独立测试行情端，两个方向都按各自结果汇总。交易阶段返回失败时，程序仍会尝试一次行情测试，方便判断故障局限在交易端还是两个方向都有问题。配置、创建对象或日志写入等严重异常则直接结束程序。

## 7. `runStage` 怎样把请求和响应连起来

下面是源码中认证阶段的调用，展示同一个编号怎样用于发送和等待：

```cpp
if (!runStage(state, log, "TRADER", Stage::Authenticate, 1, o.timeout,
              [&] { return api->ReqAuthenticate(&auth, 1); }))
    return false;
```

`[&] { ... }` 是一个 lambda，可以理解成“把发请求这个动作先包装起来，交给 `runStage` 在合适的时候执行”。`runStage` 先调用 `state.begin` 登记阶段、编号和截止时间，再执行这个动作。因此即使响应到得很快，等待状态也已经准备好了。

提交后，`state.immediate(rc)` 处理请求函数的返回值。`rc != 0` 时立即记录提交失败；`rc == 0` 时等待回调。随后 `state.wait()` 返回本阶段结果，`report` 把阶段、编号、结果、业务错误码以及必要的数据写入日志。

每个阶段默认有自己的 30 秒超时。`--timeout 60` 表示每阶段最多等待 60 秒，并不是整个程序固定 60 秒。超时后不会自动再次使用凭据重试。

## 8. `State`：请求匹配、等待和超时

### 阶段与编号同时匹配

`State::matching` 同时检查：有活动请求、请求尚未完成、阶段相同、请求编号符合当前策略、当前时间仍在截止时间之前。默认策略 `Strict` 要求回调编号等于请求编号；只有 `MdSpi` 的登录与错误回调显式使用 `AllowZero`，用于兼容 2026-09-14 实机观察到的“请求编号 1、成功回调编号 0”。

例如资金查询正在等编号 `3` 时，晚到的编号 `2` 登录响应不会把查询改成成功。即使收到正确编号，超过截止时间的响应也不会改变这次超时结论。

当前每个 API 对象只执行一轮顺序测试，因此固定的几个编号足够。行情兼容规则仍要求当前活动阶段是 MD 登录、响应在截止时间内且尚未完成；不会让编号 0 的响应完成 Trader 阶段。以后若增加持续查询或允许重新登录，需要为每个新的在途请求分配新编号，不能机械重复使用本例的固定编号。

### `bIsLast` 与空结果

`State::response` 先检查业务错误，再把有效数据复制到 `Result`。登录结构复制到 `Result::login`；资金记录逐条加入 `Result::accounts`。

查询可能返回多条资金记录，所以收到第一条时不应立即结束。代码一直等到 `bIsLast == true`，才标记响应完整。最后一条查询没有数据、也没有错误时，允许正常结束；认证和登录的最终响应缺少必要数据时则失败。

### 条件变量不会让回调等主线程

`State::wait` 使用 `std::condition_variable`。主线程等待期间，`wait_until` 会释放互斥锁，让 SDK 回调线程能够更新结果；响应完成时 `finish` 唤醒主线程。

回调只进入短临界区检查和复制数据、通知等待者，不在回调里等待另一条响应，也不在那里发送下一阶段请求。如果在 SDK 的回调线程里阻塞等待，而 SDK 又要用该线程派发后续响应，就可能造成一直等不到结果的局面。

SPI 在交给 `State` 过滤之前先记录原始回调名、实际请求编号、`bIsLast` 和响应错误，便于识别柜台不回显请求编号等情况；主线程继续负责阶段汇总和结果数据日志。`Logger` 自身使用互斥锁，避免 SDK 回调线程与主线程同时写屏幕或文件时产生数据竞争。状态结果日志仍在取得快照、释放 `State` 锁之后写入。

### 超时与系统时间

日志时间使用本机当地时间，便于和实际测试截图对应。超时使用 `std::chrono::steady_clock` 单调时钟，不会因为电脑校时而突然多等或少等。正式留证时仍应核对本机日期、时间和时区。

### “不自动重试”的准确含义

附件 SDK 的断线回调说明底层 API 可能自行重连。本工具的 `State::disconnected` 会把 `disconnected_` 锁定为真，当前测试失败；后续 `OnFrontConnected` 不会触发应用自动重发认证、登录或查询。

因此这里保证的是“应用不自动重发业务请求”，不能误解成“SDK 在任何时候都绝不会尝试重连”。需要重测时，查看本次原因后重新运行程序，得到新的日志目录。

## 9. 生命周期：为什么使用 `unique_ptr` 和 `Release`

API 对象由 SDK 的 `CreateFtdcTraderApi` / `CreateFtdcMdApi` 创建，应按 SDK 约定调用 `Release` 释放，不能直接 `delete api`。

本工程给 `std::unique_ptr` 配置 `ApiDeleter`：离开测试函数时，无论是正常完成、阶段失败提前返回还是异常退栈，都会先 `RegisterSpi(nullptr)` 再 `Release()`。

`State`、SPI 对象、前置地址字符串和请求缓冲都在 API 对象之前创建，所以退栈时 API 先释放，它们仍然活着，避免 SDK 收尾期间碰到已经销毁的对象或缓冲。回调的数据指针由 SDK 提供，不能长期保留指针；本程序在回调有效期间把需要的数据复制出来。

释放 API 在主线程执行，不在 SDK 回调里执行。`Join()` 的含义是等待 SDK 线程退出，不是“登录并等结果”；在当前一次性工具中先 `Join()` 再尝试 `Release()` 可能一直卡在等待上，所以程序直接用明确的阶段等待和最终释放。

## 10. 凭据、日志和实际运行证据

### 一次填写配置，后续自动登录

首次在工程根目录执行以下命令，将模板复制为本机文件并打开编辑：

```powershell
Copy-Item config\connection.local.ini.example config\connection.local.ini
notepad config\connection.local.ini
```

填入交易密码与认证码后保存为 UTF-8：

```ini
[credentials]
password=填写交易密码
auth_code=填写认证码
```

之后继续使用原来的 `run_windows.bat` 即可，无需新增参数或每次输入。已有本地文件时不要再复制模板覆盖；以后修改凭据只需编辑本地文件，不需要重新编译。

### `readConfig` 与凭据来源

`readConfig(path, Secrets&)` 先解析选中的 INI，并将连接参数写入 `Config`、`password` 与 `auth_code` 写入 `Secrets`；然后查找同目录的同名 `*.local.ini`。例如 `config/connection.ini` 对应 `config/connection.local.ini`，`config/demo.ini` 对应 `config/demo.local.ini`。本地文件不存在时继续使用主配置；若传入的路径已经以 `.local.ini` 结尾，则不再继续查找另一层本地配置。

凭据字段可以写在 `[credentials]` 小节，也可以直接平铺在 INI 中。对密码和认证码分别选择第一个非空值，优先级为：**同名本地 INI → 选中的主 INI → `CTP_PASSWORD` / `CTP_AUTH_CODE` → 隐藏输入**。空白配置值表示继承已有来源，不会清空另一份文件已提供的凭据。配置已经提供非空值时不再调用输入提示；缺项时才由 `getSecret` 读取相应环境变量，仍为空时关闭控制台回显并提示用户输入。行情模式 `--mode md` 不使用也不索取 AuthCode。

解析器以每行第一个 `=` 分隔键和值，并去掉值两端空白。值中的 `#`、`;` 和后续 `=` 都是字面字符；因此不要在值后面附加注释，也不需要加引号。每个文件内部仍检查重复键和未知键，不把错误行及凭据值回显到错误信息。

公开仓库保存空模板，真实 `*.local.ini` 由 `.gitignore` 排除。凭据文件在本机以明文保存。`package_release.ps1` 不包含真实本地配置，并从运行包的主配置副本中去掉 `password` / `auth_code` 行，保留空模板；接收运行包的人需要在自己的电脑填写一次。本次功能的验证范围为离线配置解析及流程检查，维护端没有执行 Windows 实机编译或真实柜台登录，不能把离线验证当作成功登录证据。

### 日志与凭据生命周期

`clean` 在输出前替换已知秘密值，并去掉换行及控制字符，避免远端错误信息伪造成新日志行。`sdkMessage` 把此套 Windows SDK 的中文错误文本按 CP936 转换成 UTF-8。`Secrets` 析构时尽量清除自己当前字符串缓冲，但无法保证操作系统环境和 SDK 内部的所有副本也被清除。

每次运行按时间、毫秒及进程编号建立新的 `logs` 和 `flow` 子目录，避免覆盖旧证据，也避免不同运行共用流文件。`flow` 是 SDK 的通信流相关文件目录，不是账户成交成功的证据；人读的测试结果主要在对应 `run.log`。

登录成功日志保留账号信息，符合本次报告截图需要。资金查询日志可能包含账户余额等实际数据；它们由此次只读查询取得，分享日志时应按用途选取范围。

`administratorStatus` 检查 Windows 当前进程是否已提升权限。程序明确记录 `physical_machine=NOT_VERIFIED`，因为“能检查管理员权限”不等于“能证明当前是合规的实体测试机”。程序不会代替券商完成终端采集核验，也不会替用户发送当天测试通知。

### 退出码

| 退出码 | 含义 |
| --- | --- |
| `0` | 所选择且实际执行的连接/基础测试通过，或正常显示帮助 / 版本 |
| `1` | 至少一个实际执行的测试阶段失败 |
| `2` | 参数、配置、凭据、文件或对象创建等流程发生异常 |

日志里的 `RESULT overall=PASS` 只指本次实际选择的范围。连接模式不能证明交易；基础 dry-run 不能证明柜台接受报单；单次实发通过也不等于附件六整体通过，仍不证明两个交易所都已完成实测、行情订阅、正式风控、录屏、手册或安全检测。

## 11. 后面怎样扩展比较清楚

后续可以沿用“主线程发起一个明确动作、SPI 接收并复制响应、状态对象关联请求、日志记录真实结果”的结构。当前 `OrderLifecycle` 已将单次委托的排队、拒绝、成交和撤单与普通 `bIsLast` 请求分开，但它不是持久订单管理器，也不查询全部委托、成交或持仓。

每日计数、每秒速率、价格检查仍需要明确作用范围和报备阈值，并接到 `ReqOrderInsert` 之前；没有这些正式参数时不能把当前 `VolumeTotalOriginal=1` 保护写成完整风控。

先用当前工具确认交易和行情接入链路，再逐项增加可验证功能，能让每次失败都对应一个清楚的问题。每增加一项实际功能，再同步更新使用说明、测试范围和证据要求。
