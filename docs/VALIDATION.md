# 交付验证记录

## v0.4.0：每秒峰值限制与等待成交

- 新增 `per_second_max_order_count`；与 `daily_max_order_count` 分别配置，两项均为 `1..999999999` 整数。实发前要求两项均已填写；普通连接测试不受此新增要求影响。
- 每秒限额使用最近连续 1000ms 窗口。`DailyOrderCounter::submit` 在同一把跨进程锁下检查每日/每秒额度、持久预占、调用 SDK 并记录调用完成时刻。任一限额拦截时不调用 SDK、不消耗另一限额，撤单不计入、不受报单限额阻止。SDK 返回失败或后续拒单仍计一次报单尝试。
- 原有两字段每日状态文件保留日计数并升级；同目录、同账号多个进程共享计数，记录当日观察到的每秒峰值。交易日切换不清空尚未到期的最近一秒记录。检测到主机 uptime 回退时，旧窗口记录保守保留一整秒。中断的提交保留 pending 标志并阻止继续报单，需核对柜台和计数；不自动重试。
- `run_risk_windows.bat settings` 显示两项阈值；`trigger` 和 `trigger-second` 分别注入已达到每日/每秒阈值的临时计数并调用同一判断函数，均明确标注离线自测、未连接/未发单，不读写真实计数。这些截图不能被描述为真实发出 N 笔委托的证据。
- 新增 `--order-goal fill --fill-wait 10`，等待成交而不是排队即撤；成交通过，未成交到期后最多申请一次撤单。撤单拒绝或无最终回报时明确提示 `residual_order=UNKNOWN`，不能宣称已清理。仍只发一张一笔、不重试不追价，沿用同一双限额入口和实发确认。
- 完整离线入口通过：既有 36 项、每秒限额 16 项、成交生命周期 15 项、Windows 凭据分支 8 项、Windows 弹窗分支 5 项及 POSIX 隐藏输入检查。覆盖跨秒边界、跨实例并发、计数迁移、超限不调用发送函数、pending 崩溃状态、回报去重、成交/撤单竞态及同账号重复报单编号的跨会话成交隔离；C++17 语法检查通过。
- Windows 分支测试使用实际源码片段及 API 测试替身，可验证编译和弹窗文本数值；维护环境没有 MSVC/Windows，未执行原生构建、真实弹窗渲染、柜台登录或报单。正式阈值、现场截图与成交结果须在用户环境核验。

## v0.3.0：每日最大报单量风控

- 新增 INI 配置 `daily_max_order_count`，只接受 `1..999999999` 的整数。实发模式若未配置，在凭据输入和网络连接前停止。
- 每次准备调用 `ReqOrderInsert` 时先按交易日持久计数；SDK 立即返回失败或柜台后续拒单也计 1 笔，撤单不计。达到阈值后在 SDK 调用前拦截，并弹出包含阈值、当前计数和未发单结果的中文提示。
- 新增 `run_risk_windows.bat settings|trigger`。设置弹窗显示正式阈值和口径；触发自测使用与实发共用的判断函数，但不读写真实计数、不索取凭据、不连网、不发单，并在弹窗/日志中明示这一点。
- 离线回归增加 6 项，覆盖配置覆盖、数值范围、自测确认令牌、阈值边界、持久与交易日重置、损坏状态默认拒绝；连同既有测试共 36 项。
- 当前维护环境不能显示 Windows `MessageBoxW`，因此弹窗外观和 MSVC 构建仍需用户拉取后在 Windows x64 实机复核。正式截图必须在 `daily_max_order_count` 已填为报备表值时现场生成。
- 用户首次 Windows 构建暴露 `MessageBoxW` 缺少 `User32.lib` 的链接错误；直接构建脚本与 CMake 入口均已加入 `user32`。Windows 环境变量读取同时改用 `_dupenv_s`，消除 MSVC 的 C4996 `getenv` 警告。
- 后续 Windows 构建暴露该修改中的局部变量重名：环境变量缓冲区与隐藏输入字符串均叫 `value`。已将缓冲区改名为 `environmentBuffer`，修复 C2371 及其引发的 C2039；不改变凭据优先级、隐藏输入或风控逻辑。
- 回归入口新增 Windows 凭据分支检查：从当前 `src/main.cpp` 提取原函数，用 Windows API 测试替身编译并执行 8 个场景。改名前复现同类编译错误，改名后 8 项、既有 36 项及 POSIX 隐藏输入检查全部通过；C++17 源码语法检查通过。该测试不依赖 SDK 库、不连接柜台，也不等同于 Windows/MSVC 原生构建或真实控制台验证。

## v0.2.2：中文 SDK 消息正常显示

- Windows SDK 返回的 GBK/CP936 `ErrorMsg` 和 `StatusMsg` 统一转换为 UTF-8，控制台与日志保留可读中文，不再显示乱码或 `NON_ASCII_OMITTED`。
- `OnRtnOrder` 继续同时记录原始状态字符与英文枚举名，中文 `status_msg` 作为柜台原始解释保留。
- 离线回归改为验证 UTF-8 日志保留及跨平台 SDK 文本处理契约；全部 30 项通过。Windows CP936 转换分支仍需在用户 Windows 实机重新构建后核验。

## v0.2.1：纯英文运行日志

- 所有运行日志在凭据脱敏后统一限制为可打印 ASCII，避免 Windows 控制台因柜台 GBK 文本出现乱码或把外部控制字符写入证据日志。
- `OnRtnOrder` 同时记录原始状态字符和英文枚举名，例如 `submit_status_code=4 submit_status=INSERT_REJECTED`、`order_status_code=5 order_status=CANCELED`。
- `ErrorMsg` 与 `StatusMsg` 若为纯 ASCII 则保留；若包含非 ASCII 字节则不输出原始文本，只标记 `NON_ASCII_OMITTED`，并保留其中连续的数字错误线索，例如 `numeric_tokens=5009`。
- 新增 3 项离线回归，验证 ASCII 输出约束、本地语言消息摘要及订单状态英文映射；连同原有测试共 30 项通过，POSIX 隐藏输入检查通过。

## v0.2.0：1.1 基础功能测试

- 新增 `--test basic` 的 `single-shot-limit` 单次策略：使用明确的合约、交易所、方向、开平和限价生成一笔 `VolumeTotalOriginal=1` 的 GFD 限价单。
- 默认是 dry-run；只有同时提供 `--send-order --confirm SEND_ONE_ORDER` 才调用 `ReqOrderInsert`。程序没有数量参数、自动重试或追价。
- 实发模式按本次 `OrderRef` 处理 `OnRspOrderInsert`、`OnErrRtnOrderInsert`、`OnRtnOrder`、`OnRtnTrade`、`OnRspOrderAction` 和 `OnErrRtnOrderAction`；排队后自动申请撤单。完全成交不会被写成撤单通过。
- 新增 5 项离线回归，覆盖默认不实发及确认令牌、报单字段、排队后程序撤单、完全成交不得冒充撤单通过，以及撤单调用返回前并发回调不能掩盖即时提交失败；连同原有测试共 27 项通过，POSIX 隐藏输入检查通过。
- 同一组 27 项离线回归通过 AddressSanitizer 与 UndefinedBehaviorSanitizer；当前容器按既有方式关闭 LeakSanitizer，不能据此宣称已证明无内存泄漏。
- `src/main.cpp` 使用实际 3.7.5 头文件通过 C++17 语法检查，并与附件中 Linux x86-64 的两只 3.7.5 原厂 `.so` 完成链接；离线运行 `--version` / `--help` 显示匹配版本且未建立连接。当前环境没有 Windows/MSVC，未执行 Windows 原生链接，也未向柜台发送订单。
- 正式基础功能结论仍待用户在指定 Windows 实体机上，使用券商确认的当日有效 SSE/SZSE 测试合约和价格分别运行；dry-run 不能作为柜台报单或撤单证据。

## v0.1.3：兼容行情登录回调请求编号 0

2026-09-14 13:05:41 的 Windows x64 实机最小对照程序使用 `v3.7.5_CP_20251125` 连接 `tcp://101.226.254.157:32213`，调用 `ReqUserLogin(..., 1)` 后立即收到 `OnRspUserLogin`：`request_id=0`、`bIsLast=1`、`ErrorID=0`。原程序因严格要求回调编号等于 1 而忽略该成功响应，最终误报超时。

- 修复仅允许 MD 登录及其 `OnRspError` 在当前活动登录阶段接受回调编号 0；Trader 认证、登录和资金查询继续严格匹配请求编号。
- SPI 在状态过滤前写入原始回调名、实际请求编号、`bIsLast` 和响应错误；`Logger` 增加互斥保护，以支持 SDK 回调线程与主线程安全写日志。
- 新增三项回归：MD 登录接受编号 0、MD 错误响应接受编号 0、严格策略拒绝编号 0 并保留正确编号的响应。
- `python3 tests/run_config_tests.py` 共 22 项离线回归和 1 项 POSIX 隐藏输入检查通过；`src/main.cpp` 使用实际 3.7.5 头文件通过 C++17 语法检查。
- 用户已在 Windows x64 实体机重新构建 v0.1.3，并于 2026-09-14 13:25:42 连接当前 MD 前置实测通过：`ReqUserLogin(..., 1)` 的成功回调编号仍为 `0`，程序记录兼容原因并最终输出 `overall=PASS md=PASS`。

## v0.1.2：个股期权 SDK 升级到 3.7.5

本次将 Windows x64 SDK 从 `v3.7.0_CP_20231127` 整体升级到 `v3.7.5_CP_20251125`，没有改变连接、认证、登录或资金查询的业务顺序。

- 公开仓库中 `sdk/win64` 的四个头文件、两只 LIB 及 `error.dtd` / `error.xml` 与用户上传压缩包内的 x64 目录逐字节一致。
- 两只本地 DLL 已核验为 PE32+ x86-64，内嵌版本为 `v3.7.5_CP_20251125  9:30:02.f7e78374`，但不提交到公开 Git；两只 LIB 的对象成员也均为 x86-64。
- `sdk/include` 的四个 UTF-8、LF 换行头文件副本与对应原始 GB18030 头文件解码后的文本一致，没有改变声明。
- `src/main.cpp` 使用 3.7.5 头文件通过 Linux g++ C++17 语法检查；这不等于 Windows MSVC 链接或原生 DLL 加载验证。
- `python3 tests/run_config_tests.py` 的 19 项离线配置检查和 1 项 POSIX 终端隐藏输入检查通过。测试不建立网络连接，也不能证明真实账号能够登录。
- 已核对 3.7.5 与 3.7.0 的行情登录接口和请求/响应结构一致，但其他 Trader 虚函数和共享结构存在 ABI 变化，因此本次始终按完整配套文件升级。
- 本环境未执行 Windows 原生编译、加载 DLL 或真实柜台登录；仍需先把两只配套 3.7.5 DLL 从原包复制到本地 `sdk/win64`，再在指定 Windows x64 实体机重新构建，并确认 `run_windows.bat --version` 的 Trader 和 MD 均显示 3.7.5 后复测。

仓库根目录 `SHA256SUMS.txt` 为当前工作树文件清单；下面保留的 v0.1.1 和 v0.1.0 内容只对应各自历史版本。

## v0.1.1：配置凭据读取

本次修改增加 `password` / `auth_code` 配置和同目录 `*.local.ini` 自动叠加，未执行真实柜台连接。以下检查使用合成凭据，不能当作实测登录凭证：

- `python3 tests/run_config_tests.py`：19 项离线配置检查和 1 项 POSIX 终端隐藏输入检查通过。覆盖主配置/本地配置/环境变量优先级、空值继承、两项凭据独立取值、UTF-8 BOM、特殊字符、错误和日志脱敏、SDK 字段长度限制。
- 另用离线假 SDK 执行 6 项完整启动检查：本地配置的 `all` 模式、主配置的交易模式、行情仅配置密码、配置与环境变量混合、原有环境变量模式、行情登录失败。检查了传给假 SDK 的凭据值、无多余输入提示、退出码和日志不含凭据；假 SDK 不建立网络连接。
- `--help` 和 `--version` 在没有配置和凭据时仍可运行，不创建 SDK 对象。
- 核对本机配置被 Git 忽略、配置模板为空；源码审查确认运行包剥离主配置中的凭据行且不复制实际 `*.local.ini`。
- 本环境未执行 Windows 原生编译、Windows 隐藏输入或 PowerShell 打包。Windows 测试入口已提供，但仍需本机验证；原有 SDK 和连接流程保持不变。

## v0.1.0：原始交付记录

本次验证检查应用代码的正确性和 SDK 配套情况。没有在本环境执行 Windows 原生编译、加载原生 DLL、连接中信服务器、认证或登录测试。正式实机结果仍为待执行。

## 已完成

- 原始 Windows 64 位 SDK 的 10 个文件与用户压缩包内对应文件逐字节一致。
- 编译使用的 4 个 UTF-8 头文件副本与原始 GBK 头文件解码后的文本一致，未改变字段和声明。
- `src/main.cpp` 使用实际接口头文件通过 Linux g++ C++17 语法检查；这不等于 MSVC 链接成功。
- 使用按实际头文件实现的离线假后端验证 20 个应用逻辑场景，全部符合预期。假后端不建立网络连接，不使用实际账号密码，并将未预期的 API 调用判为失败。
- 最终源码在上述离线测试中通过 AddressSanitizer 与 UndefinedBehaviorSanitizer 检查。容器限制下未启用 LeakSanitizer，不能据此宣称已证明无内存泄漏。
- 人工核对 Windows 构建、运行与打包脚本路径及参数；本环境没有 Windows / PowerShell，脚本仍需本机执行验证。

## 离线应用逻辑覆盖

| 场景组 | 实际检查 |
| --- | --- |
| 正常路径 | 交易与行情按顺序完成，结果各自汇总 |
| 认证 | 业务失败、提交失败、错误编号、错误编号后正确响应、必需响应为空 |
| 登录 | 业务失败、错误编号、认证旧响应在登录阶段迟到、必需响应为空 |
| 连接与断线 | 连接超时、登录时断线后重连/迟到成功不能改为通过 |
| 资金查询 | 空结果正常结束、多条记录等待最后一条、错误编号、业务失败、断线 |
| 行情登录 | 业务失败、错误编号、必需响应为空 |

失败和错误编号场景确认不会错误推进后续请求。查询没有记录会明确显示空结果；不会编造资金行。假后端的成功只代表程序正确处理了模拟响应，不是柜台成功凭证。

被验证的源码 SHA256：

```text
c52633ead8d30dc02d7b862c662d6afe49adbe7a4ae31b63a8e4750a24355fdf
```

离线假后端、假数据及其日志没有混入正式运行程序包，避免被误作实测证据。构建入口只链接用户提供的 Windows SDK。

## Windows 实体机待执行

1. `build_windows.bat` 成功生成 x64 EXE 并复制配套 DLL。
2. `run_windows.bat --version` 记录实际加载的两套 API 版本。
3. 以管理员身份运行 `run_windows.bat --mode trader`，确认前置、认证、登录、资金查询各步结果。
4. 运行 `run_windows.bat --mode md`，单独核验行情前置和登录。
5. 保存真实账号登录截图及日志；登录当天及时通知中信核验终端信息。

此阶段还没有正式图文手册、实测截图、录屏、交易成交或风控通过证据，也没有病毒检测结果。后续项目见 `REPORT_ROADMAP.md`。
